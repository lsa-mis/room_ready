<h1>New rover</h1>
<%# no need to use this page anymore %>
<% flash.each do |key, value| %>
    <% if key == 'alert' %>
      <%= content_tag(:div, value, class: "flash #{key}", id: "#{key}") %>
    <% end %>
<% end %>
<%= render "form", rover: @rover %>

<br>

<div>
  <%= link_to "Back to rovers", rovers_path %>
</div>
