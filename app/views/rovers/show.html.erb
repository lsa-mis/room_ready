<p style="color: green"><%= notice %></p>

<%= render "rover", rover: @rover %>

<div>
  <%= link_to "Edit this rover", edit_rover_path(@rover) %> |
  <%= link_to "Back to rovers", rovers_path %>

  <%= button_to "Delete this rover", @rover, method: :delete, data: { turbo_confirm: 'Are you sure to delete this rover?' } %>
</div>
