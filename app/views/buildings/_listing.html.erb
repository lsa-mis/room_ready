<%= turbo_frame_tag 'all_buildings' do %>  
  <table class="table text-center">
    <thead class="table-group-divider">
      <tr>
        <th>Building Record Number</th>
        <th>Name</th>
        <th>Nickname</th>
        <th>Address</th>
        <th>Zone</th>
        <th>View</th>
        <th>Edit</th>
        <th>Remove</th>
      </tr>
    </thead>
    <tbody class="table-group-divider">
      <% @buildings.each do |building| %>
        <tr class="building_tbody_tr">
          <td class="building_tbody_td">
            <%= building.bldrecnbr %>
          </td>
          <td class="building_tbody_td">
            <%= building.name.titleize %>
          </td>
          <td class="building_tbody_td">
            <%= building.nick_name %>
          </td>
          <td class="building_tbody_td">
            <%= building.full_address %>
          </td>
          </td>
          <td class="building_tbody_td">
            <%= show_zone(building) %>
          </td>
          <td class="building_tbody_td">
            <%= link_to building_path(building), 'data-bs-toggle': "tooltip", 'data-bs-placement': "bottom", 'title': "View", data: { turbo_frame: "_top" } do%>
            <i class="bi bi-eye-fill text-primary"></i>
            <% end %>
          </td>
          <td class="building_tbody_td">
            <%= link_to edit_building_path(building), 'data-bs-toggle': "tooltip", 'data-bs-placement': "bottom", 'title': "Edit", data: { turbo_frame: "_top" } do %>
            <i class="bi bi-pencil-square text-primary"></i>
            <% end %>
          </td>
          <td class="building_tbody_td">
            <%= link_to "#", 'data-bs-toggle': "tooltip", 'data-bs-placement': "bottom", 'title': "Archive" do %>
            <i class="bi bi-trash-fill text-danger"></i>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
