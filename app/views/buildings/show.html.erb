<p style="color: green"><%= notice %></p>

<%= render @building %>

<div>
  <%= link_to "Edit this building", edit_zone_building_path(@building[:zone_id], @building) %> |
  <%= link_to "Back to buildings", zone_buildings_path %>

  <%= button_to "Destroy this building", @building, method: :delete %>
</div>
