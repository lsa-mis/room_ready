<div class="d-flex flex-row align-items-center justify-content-between">
  <h1>Number of Room Issues Report</h1>
  <%= link_to 'Back to Reports', reports_path, class: "link_to" %>
</div>
<p>This report shows a number of issues per room.</p>

<div class="py-3">
  <%= render 'filter_form', report_url: number_of_room_issues_report_reports_path %>

  <% if params[:commit] && params[:format] == 'html' %>
    <%= render 'reports_table' %>
  <% end %>
</div>
