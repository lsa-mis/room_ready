<tr id="<%= dom_id(room_ticket) %>">
  <td class="text-start"><%= room_ticket.description %></td>
  <td>
    <%= room_ticket.submitted_by %>
  </td>
  <td>
    <%= room_ticket.created_at.strftime('%m/%d/%Y') %>
  </td>
</tr>
